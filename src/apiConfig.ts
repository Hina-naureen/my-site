/**
 * Centralized API base URL.
 * Update BACKEND_URL after deploying backend to Render.
 */
export const BACKEND_URL = 'https://physai-backend.onrender.com';
export const API_BASE = `${BACKEND_URL}/api`;
