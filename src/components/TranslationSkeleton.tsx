/**
 * TranslationSkeleton — no-op.
 * Urdu mode is now a client-side RTL layout toggle with no API calls,
 * so there is no loading state to display.
 */
export default function TranslationSkeleton(): null {
  return null;
}
